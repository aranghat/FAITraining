

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 
        <h1>Hello MVC</h1>
        <a href="/home/aboutus">About Us</a>
       Current Time in server is @DateTime.Now

        @*@for(int i=0;i< ViewBag.MaxCount; i++)
        {
            <div>@i</div>
        }

        <form action="/home/index" method="post">
            <input type="text" name="message" />
            <button type="submit">Sent the message</button>
        </form>

        @if (ViewBag.NewResponse != null)
        {
           <h1>@ViewBag.NewResponse</h1>
        }*@

        <form action="/home/index" method="post">
            <input type="number" name="ctc" />
            <select name="gender">
                <option value="Male">Male</option>
                <option value="Female">Female</option>
            </select>
            <button type="submit">Calculate Tax</button>
        </form>

        @if(ViewBag.Tax != null)
        {
            <h1>Total Tax : @ViewBag.Tax</h1>
        }
    </div>
</body>
</html>
